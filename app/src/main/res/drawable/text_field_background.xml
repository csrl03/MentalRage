<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Gradiente -->
    <gradient
        android:startColor="#DCDCDC"
        android:endColor="#F6F6F6"
        android:angle="30" />

    <!-- Bordes redondeados -->
    <corners android:radius="15dp" />

    <!-- Borde -->
    <stroke
        android:width="4dp"
        android:color="#7A1818" />
</shape>
